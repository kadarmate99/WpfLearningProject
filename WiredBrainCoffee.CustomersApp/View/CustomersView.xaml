<UserControl x:Class="WiredBrainCoffee.CustomersApp.View.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WiredBrainCoffee.CustomersApp.View" 
             xmlns:converter="clr-namespace:WiredBrainCoffee.CustomersApp.Converter"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="500" d:FontSize="16">
    <UserControl.Resources>
        <converter:NavigationSideToGridColumnConverter 
      x:Key="NavigationSideToGridColumnConv"/>
        <DataTemplate x:Key="CustomerDataTemplate">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding FirstName, ValidatesOnNotifyDataErrors=False}"
                           FontWeight="Bold"/>
                <TextBlock Text="{Binding LastName}" Margin="5 0 0 0"/>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Customer list -->
        <Grid Grid.Column="{Binding NavigationSide,
      Converter={StaticResource NavigationSideToGridColumnConv}}"
          Background="#777" Width="260">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal">
                <Button Margin="10" Width="75" Command="{Binding AddCommand}">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="/Images/add.png" Height="18" Margin="0 0 5 0"/>
                        <TextBlock Text="Add"/>
                    </StackPanel>
                </Button>
                <Button Content="Delete" Width="75" Margin="0 10 10 10"
                Command="{Binding DeleteCommand}"/>
                <Button Margin="0 10 10 10" Command="{Binding MoveNavigationCommand}">
                    <Image Source="/Images/move.png" Height="18"/>
                </Button>
            </StackPanel>
            <ListView ItemsSource="{Binding Customers}"
                SelectedItem="{Binding SelectedCustomer,Mode=TwoWay}"
                ItemTemplate="{StaticResource CustomerDataTemplate}"
                Grid.Row="1" Margin="10 0 10 10"/>
        </Grid>

        <!-- Customer detail -->
        <Grid Grid.Column="1" 
              Margin="10"
              Visibility="{Binding IsCustomerSelected,
                Converter={StaticResource BooleanToVisibilityConv}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel>
                <Label>Firstname:</Label>
                <TextBox Text="{Binding CurrentCustomerEdit.FirstName,
                Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
                <Label>Lastname:</Label>
                <TextBox Text="{Binding CurrentCustomerEdit.LastName,
                Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
                <CheckBox IsChecked="{Binding CurrentCustomerEdit.IsDeveloper,
                Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                Margin="0 20 0 0">
                    Is developer
                </CheckBox>
            </StackPanel>
            <StackPanel  Grid.Row="2"
                         Orientation="Horizontal"
                         HorizontalAlignment="Center">
                <Button Width="100"
                        Command="{Binding RevertCommand}">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="/Images/revert.png" Height="18" Margin="0 0 5 0"/>
                        <TextBlock Text="Revert"/>
                    </StackPanel>
                </Button>
                <Button Width="100"
                        Margin="10 0 0 0"
                        Command="{Binding SaveCommand}">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="/Images/save.png" Height="18" Margin="0 0 5 0"/>
                        <TextBlock Text="Save"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
